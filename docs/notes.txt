- don't repeat unnecessary operations inside of the loop
- optim. options: -O1,2,3,S
- stack vs. heap
- dynamic memory allocation
- pointer is a variable, stores an address to a memory
    - int *x = new int*[500] (C++)

- k-means clustering
- empty clusters
    - accept them as they are?
    - greedy algorithm